scorer_name: "new_heuristic_scorer"
scorer_type: "user"
scorer_short_description: "TEST HEURISTIC"
scorer_long_description: "TEST HEURISTIC"
scorer_display_name: "TEST HEURISTIC"
scorer_variables: []
score_type: "Double"
scorer_function: |
  /**
   * IMPORTANT:
   * Changing the name and arguments of the function will cause failures in output score generation. Please modify the code
   * within the function only.
   */

  /**
   * A function that processes language model interactions.
   * @param {string} llmInput - The input data provided to the language model.
   * @param {Object} llmContext - The contextual information associated with the input that may affect processing.
   * @param {string} llmOutput - The output generated by the language model based on the input and context.
   * @param {string} groundTruth - The actual, verified response or data that is considered correct for the given input.
   * @param {Object} metaData - Additional data passed by the user, which can be of any form and structure.
   * @returns {number} A numerical value between 0 and 1 inclusive, representing some measure of effectiveness or accuracy.
   */
  const main = (llmInput, llmContext, llmOutput, groundTruth, metaData) => {
    // Function implementation goes here.

    // Example return, actual implementation needed based on computation.
    return 0; 
  };
output_categories:
  - color: "#6EB1FF"
    start_range: 0
    end_range: 0
